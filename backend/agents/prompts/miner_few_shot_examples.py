"""
Miner Agent Few-shot 案例库
提供高质量的正反案例，帮助模型学习正确的提取方式
"""

# ============================================================================
# Few-shot 案例：好案例 vs 坏案例对比
# ============================================================================

FEW_SHOT_EXAMPLES = """
## 📚 学习案例（请严格模仿好案例，避免坏案例的错误）

### 案例 1：长答案提取 - 保留完整上下文
**原文**：
面试官问了 Redis 持久化，我说了 RDB 和 AOF，RDB 是定时快照，优点是恢复快，缺点是可能丢数据；AOF 是实时日志，优点是数据安全，缺点是文件大恢复慢。

✅ **好案例**（完整保留原文信息）：
```json
{
  "question_text": "请介绍 Redis 的持久化机制",
  "answer_text": "Redis 有 RDB 和 AOF 两种持久化方式。RDB 是定时快照机制，优点是恢复速度快，缺点是可能丢失最后一次快照后的数据；AOF 是实时日志记录，优点是数据安全性高，缺点是文件较大且恢复较慢",
  "difficulty": "medium",
  "question_type": "工程类",
  "topic_tags": ["Redis", "持久化", "RDB", "AOF"]
}
```

❌ **坏案例**（只提取关键词，丢失上下文）：
```json
{
  "answer_text": "RDB、AOF"  // 太简短，无参考价值
}
```

---

### 案例 2：无答案时的补充 - 提供完整参考答案
**原文**：
面试官问了 Redis 的过期键删除策略，我没答上来。

✅ **好案例**（补充详细参考答案）：
```json
{
  "question_text": "请介绍 Redis 的过期键删除策略",
  "answer_text": "Redis 的过期键删除策略包含三种：1. 惰性删除：访问键时才检查是否过期，节省 CPU 但可能内存泄漏；2. 定期删除：每隔一段时间扫描过期键，平衡 CPU 和内存；3. 内存淘汰：内存不足时触发，按策略（如 LRU）删除过期/非过期键",
  "difficulty": "medium",
  "question_type": "工程类",
  "topic_tags": ["Redis", "过期键", "删除策略"]
}
```

❌ **坏案例**（空答案或敷衍）：
```json
{
  "answer_text": ""  // 空答案无意义
}
```
或
```json
{
  "answer_text": "惰性删除、定期删除"  // 只有关键词，无解释
}
```

---

### 案例 3：多题分隔 - 必须拆分为独立条目
**原文**：
问了 Redis 持久化；MySQL 索引原理；手撕 LRU

✅ **好案例**（拆分为 3 道独立题目）：
```json
[
  {
    "question_text": "请介绍 Redis 的持久化机制",
    "answer_text": "Redis 有 RDB 和 AOF 两种持久化方式...",
    "topic_tags": ["Redis", "持久化"]
  },
  {
    "question_text": "请介绍 MySQL 索引的实现原理",
    "answer_text": "MySQL 索引以 B+ 树为主，哈希索引为辅...",
    "topic_tags": ["MySQL", "索引", "B+树"]
  },
  {
    "question_text": "请手写 LRU 缓存算法",
    "answer_text": "LRU 缓存可以用哈希表 + 双向链表实现...",
    "topic_tags": ["算法", "LRU", "哈希表"]
  }
]
```

❌ **坏案例**（合并为一道题）：
```json
[
  {
    "question_text": "问了 Redis 持久化、MySQL 索引原理、手撕 LRU",
    "answer_text": "..."  // 错误：应该拆分为 3 道题
  }
]
```

---

### 案例 4：系统设计题 - 保留架构细节和技术选型
**原文**：
如何设计一个高并发订单系统？我从架构分层、存储设计、并发控制这几个方面回答，用了 Kafka 削峰、Redis 分布式锁、MySQL 分库分表。

✅ **好案例**（保留技术细节和实现逻辑）：
```json
{
  "question_text": "请设计一个高并发订单系统",
  "answer_text": "设计高并发订单系统需从以下几个方面入手：\n1. 架构分层：采用微服务架构，拆分订单服务、支付服务、库存服务，服务间通过 Dubbo/gRPC 通信\n2. 峰值削峰：引入 Kafka 消息队列，订单提交先写入消息队列，再异步消费处理，避免峰值直接冲击数据库\n3. 并发控制：采用 Redis 分布式锁（Redlock）解决订单并发创建、库存扣减的竞争问题\n4. 存储设计：MySQL 分库分表，热点数据存入 Redis 集群，历史订单存入 HBase",
  "difficulty": "hard",
  "question_type": "工程类",
  "topic_tags": ["分布式系统", "高并发", "订单系统", "Kafka", "Redis"]
}
```

❌ **坏案例**（只提关键词，丢失实现细节）：
```json
{
  "answer_text": "用 Kafka、Redis、MySQL"  // 错误：缺少架构设计和实现逻辑
}
```

---

### 案例 5：纯题目列表（无答案）- 必须补充参考答案
**原文**：
1. 请介绍 Redis 持久化
2. MySQL 索引原理
3. 手撕 LRU

✅ **好案例**（逐条提取，补充简短参考答案）：
```json
[
  {
    "question_text": "请介绍 Redis 的持久化机制",
    "answer_text": "Redis 有 RDB 和 AOF 两种持久化方式。RDB 是定时快照，AOF 是实时日志，各有优缺点",
    "difficulty": "medium",
    "question_type": "工程类",
    "topic_tags": ["Redis", "持久化"]
  },
  {
    "question_text": "请介绍 MySQL 索引的实现原理",
    "answer_text": "MySQL 索引以 B+ 树为主，哈希索引为辅，需要注意聚簇索引和非聚簇索引的区别",
    "difficulty": "hard",
    "question_type": "工程类",
    "topic_tags": ["MySQL", "索引", "B+树"]
  },
  {
    "question_text": "请手写 LRU 缓存算法",
    "answer_text": "LRU 缓存可以用哈希表 + 双向链表实现，操作的时间复杂度为 O(1)",
    "difficulty": "medium",
    "question_type": "算法类",
    "topic_tags": ["算法", "LRU", "哈希表"]
  }
]
```

❌ **坏案例**（拒绝提取或答案为空）：
```json
// 错误 1：拒绝提取
"抱歉，原文没有提供答案，我无法提取"

// 错误 2：答案为空
[
  {
    "question_text": "请介绍 Redis 的持久化机制",
    "answer_text": ""  // 错误：必须补充参考答案
  }
]
```

---

### 案例 6：公司信息提取 - 优先从标题提取
**标题**：字节跳动后端三面面经
**正文**：面试官问了...

✅ **好案例**（从标题精准提取公司全称）：
```json
{
  "company": "字节跳动",
  "position": "后端开发"
}
```

❌ **坏案例**（模糊表述或随意填写）：
```json
{
  "company": "大厂",  // 错误：应填写具体公司名
  "position": ""
}
```

---

## 🎯 核心规则总结

1. **答案长度**：至少 20 字，包含定义 + 核心原理 + 应用场景
2. **多题分隔**：分号/换行/序号分隔的题目，必须拆分为独立条目
3. **无答案补充**：原文无答案时，必须补充结构化参考答案（定义 + 原理 + 应用）
4. **保留细节**：系统设计题必须保留架构分层、技术选型、实现逻辑
5. **公司信息**：优先从标题提取公司全称，无则填空字符串（禁止填"大厂"等模糊词）
6. **禁止幻觉**：只从原文和 OCR 结果提取，禁止从示例中编造题目
"""


def get_few_shot_examples() -> str:
    """返回 Few-shot 案例库"""
    return FEW_SHOT_EXAMPLES
