# ============================================================
# 面经 Agent — Python 依赖清单
# 安装命令（在 NewCoderAgent conda 环境中）：
#   pip install -r requirements.txt
#
# spaCy 语言模型需额外安装：
#   python -m spacy download zh_core_web_sm
#   python -m spacy download en_core_web_sm
# ============================================================

# ── Web 框架 ─────────────────────────────────────────────────
fastapi==0.133.0
uvicorn==0.41.0
pydantic==2.12.5
pydantic-settings==2.10.1
python-multipart>=0.0.9

# ── HTTP 客户端 ───────────────────────────────────────────────
requests==2.32.5
httpx==0.28.1
aiohttp==3.13.3

# ── 数据库驱动 ───────────────────────────────────────────────
neo4j==6.1.0
qdrant-client==1.17.0

# ── LLM & Embedding ──────────────────────────────────────────
openai==1.109.1           # 兼容 Volcengine / DashScope OpenAI 接口
dashscope>=1.14.0         # 阿里云 DashScope SDK（text-embedding-v4）
hello-agents==1.0.0       # hello_agents 框架（SimpleAgent / ReActAgent / PlanSolveAgent）

# ── 向量 & NLP ───────────────────────────────────────────────
sentence-transformers==5.2.3
transformers==4.57.6
# torch：勿在此 pin 死版本。CPU 环境可事后执行 pip install torch==2.10.0。
# Windows + GPU + Unsloth 微调：须先安装 2.5.1+cu124（2.6 与 triton-windows 不兼容，会无法 import Unsloth），见项目说明与同目录下注释「GPU 微调」。
accelerate==1.12.0
huggingface_hub==0.36.2
scikit-learn==1.8.0
numpy==2.4.2
spacy==3.8.11
spacy-pkuseg==1.0.1       # 中文分词

# ── 爬虫 & 解析 ──────────────────────────────────────────────
beautifulsoup4==4.14.0
lxml==6.0.2
fake-useragent==1.5.1

# ── 图像处理（OCR 本地识别，不依赖 LLM）──────────────────────
pillow==10.4.0
easyocr>=1.7.0            # 本地 OCR，支持中英文，无需 Vision API
anthropic>=0.39.0         # Anthropic Claude API（用于 Claude Vision OCR）

# ── 日志（终端彩色：INFO 绿 / WARNING 黄 / ERROR 红）────────────────
loguru>=0.7.0

# ── 环境配置 ─────────────────────────────────────────────────
python-dotenv==1.2.1

# ── 可选：题库乱码修复（cleanup_garbled_questions）更强编码纠正 ──
# ftfy>=6.0.0

# ── MCP 服务器（content-extractor 等）──────────────────────
fastmcp>=2.0.0

# ── 可选：Unsloth 本地 LoRA 微调（与「生成并训练」后台脚本一致）────────────
# 1) 需要 NVIDIA GPU + CUDA 版 PyTorch。Windows 请用 PyTorch 2.5.1+cu124（不要用 2.6+，与 triton-windows 冲突）。
# 2) 国内用阿里云（先装 torch 三件套，再装 unsloth；勿再 pip 安装 torchao；xformers 无匹配 win 轮时可不装）：
#      pip install torch==2.5.1+cu124 torchvision==0.20.1+cu124 torchaudio==2.5.1+cu124 -i https://mirrors.aliyun.com/pypi/simple/ -f https://mirrors.aliyun.com/pytorch-wheels/cu124/ --default-timeout 300
#      pip install unsloth -i https://mirrors.aliyun.com/pypi/simple/
# 3) 拉取 HuggingFace 基座模型：训练脚本默认 HF_ENDPOINT=https://hf-mirror.com；可改环境变量或设官方源。
# 4) 首次下载模型体积大、耗时长；验证链路可设环境变量 FINETUNE_MAX_STEPS=10 只跑少量步。
# 5) 官方源： https://pytorch.org/get-started/locally/
